apply plugin: "war"

bootWar {
    mainClassName = "org.regitiny.catiny.uaa.CatinyUaaApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
